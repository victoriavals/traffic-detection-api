# =============================================
# Traffic Counter API — Python Dependencies
# =============================================
# Install: pip install -r requirements.txt
#
# Untuk VPS tanpa GPU (CPU-only PyTorch):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Untuk lokal dengan NVIDIA GPU (CUDA):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# =============================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
websockets>=14.0

# --- AI / Computer Vision ---
ultralytics>=8.3.0
supervision>=0.25.0
opencv-python-headless>=4.10.0
numpy>=1.26.0
torch>=2.0.0

# --- HTTP Client (EZVIZ Cloud API, video URL download) ---
httpx>=0.27.0

# --- Environment & Config ---
python-dotenv>=1.0.0

# --- Database (Activity logs & stats) ---
motor>=3.6.0

# --- Authentication (JWT + password hashing) ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- System (ffmpeg required for video re-encoding) ---
# apt install ffmpeg  (Linux)
# choco install ffmpeg (Windows)
